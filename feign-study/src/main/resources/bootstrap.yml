spring:
  cloud:
    config:
      name: feign
      label: master
      discovery:
        enabled: true
        service-id: configserver
  profiles:
    active: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
boot: boot test