server:
  port: 8761
eureka:
  client:
    # 不在注册中心注册自己
    register-with-eureka: false
    # 不同步其他的注册中心数据
    fetch-registry: false
    # 注册中心访问地址
    service-url:
      defaultZone: http://localhost:8761/eureka/